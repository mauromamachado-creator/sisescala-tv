# 2026-03-07 — Sessão noturna

## Healthcheck OpenClaw
- Auditoria de segurança completa — 0 critical, 0 warn
- Corrigido: host-header fallback (DNS rebinding), rate limiting, permissões de pasta
- Configurado allowedOrigins pro painel de controle

## SisGOPA — Auditoria de segurança
- Token GitHub exposto no código-fonte (PENDENTE — precisa Cloudflare Worker ou token fine-grained)
- Planilhas Google acessíveis por link (PENDENTE)
- Senha ESCALA bypassável via console (PENDENTE — futuro)

## SisGOPA — Correções
- METAR internacional: regex mudado de SB\w{2} → [A-Z]{4} (OMAA, KJFK etc agora funcionam)
- OM 92/GTE-1/2026 processada (FAB 2101, SBBR→SBSC→SBGL→SBSP→SBBR, 08/03 ADET)
- Fix crash renderScreenDiarias: campo data_om→data + formato YYYY-MM-DD
- Fix formato ETD/ETA da OM 92: só hora → data+hora padronizado
- Fix tripulantes no status sempre zero: pilotData é objeto, não array
- Escala marcada como fonte crítica no status
- 3 issues GitHub processadas e fechadas (#1, #2, #3)

## Cron ajustado
- SisGOPA Error Report Processor: timeout 600→900s, prompt simplificado (grep em vez de ler arquivo inteiro)

## Preferências do Mauro
- Não usar jargão técnico (deploy, commit, regex etc) — falar direto
- ADET = A Determinar (horário de voo ainda não definido)
- SISGOPA = sistema que estamos criando juntos (não sistema externo)

## SisGOPA — Correções (continuação ~19h)
- OM 95/GTE-1/2026: tripulação atualizada — Celeste saiu, T1 Silva Junior entrou (versão 07/03 14:51)
- Regra importante: quando tem OM duplicada no Drive, SEMPRE usar a mais recente (precisa implementar no processamento automático)
- Status do sistema: fontes críticas agora são 4 (Disponibilidade, Lançamentos, Horas Anual, Escala)
- Também tem OM 94 e 96 duplicadas no Drive (2 versões cada) — verificar se estão atualizadas

## Pendências acumuladas
1. Token GitHub exposto no frontend (CRÍTICO) — Cloudflare bloqueou signup, alternativa: fine-grained token do GitHub
2. Lógica de "usar OM mais recente" no cron de checar Drive
3. Planilhas Google com dados sensíveis acessíveis por link
4. Cron de checar OMs não consegue fazer push pro GitHub sozinho (falta Git configurado na sessão isolada)

## SisGOPA v4.8 — Correções extras (~19:25)
- Ordens Especiais agora aparecem no modal de detalhes da OM (campo `ordens_especiais` no JSON)
- Versão atualizada pra v4.8 com changelog
- PDF Diárias: função dos tripulantes não aparecia — fix: busca por nome nos cartões quando não tem trigrama
- Lógica: trigrama → nome → fallback (posto CP/MJ/TC = "Piloto") — CP J. Oliveira (JOL) é Mecânico, cartões retornam correto
- OM 95 atualizada: 2S Celeste saiu, T1 Silva Junior entrou (versão 07/03 14:51)
- Regra: OMs duplicadas no Drive → sempre usar a mais recente

## Observações importantes
- CP J. OLIVEIRA (JOL) é **Mecânico**, não piloto — posto CP não garante que é piloto
- cartoesData tem 82 registros com trigrama e função de cada tripulante

## NOTAM — fontes e prioridade
- **1º AISWEB (DECEA)** — grátis, ilimitado, só Brasil (POST scraping)
- **2º aviationweather.gov** — morto atualmente (endpoint removido), verificar periodicamente
- **3º ICAO API** — fallback final, 100 chamadas grátis, NÃO GASTAR à toa
  - Registrado com machadommam@fab.mil.br, chave pendente no e-mail
  - API: `realtime-notams` (param: api_key, format, ICAOCode)
  - Usar APENAS quando AISWEB falhar ou pra aeródromos internacionais (OMAA, KJFK etc)
- Login.gov: mauromamachado@yahoo.com.br (conta ativa, 2FA por authenticator app)
- FAA API: conta existe mas "User is not assigned to this application"

## E-mails do Mauro
- mauromamachado@yahoo.com.br (pessoal)
- machadommam@fab.mil.br (institucional FAB)

## ICAO API — Ativação e teste (~20:09-20:14 EST)
- Chave ICAO: `0957b31c-6510-4974-8342-1a85ffd29a6e` — ATIVADA no portal
- Registrada com email `machadommam@fab.mil.br`
- Endpoint correto: `https://dataservices.icao.int/api/realtime-notams?api_key=KEY&format=json&ICAOCode=ICAO`
- ⚠️ `applications.icao.int` redireciona pra `dataservices.icao.int` e **lowercase nos params** — usar URL direta
- API doc7910 funciona (testei SBBR) — chave confirmada ativa
- `realtime-notams` retorna lista vazia pra SBBR/KJFK/OMAA — serviço instável ou vazio
- `notams-list` dá 502 Bad Gateway
- 100 chamadas grátis de trial

## NOTAM — Hierarquia de fontes atualizada
### Bot Telegram (bot.py)
1. AISWEB (DECEA POST) — grátis, ilimitado, só Brasil
2. ICAO API (`fetch_notam_icao`) — fallback internacional
3. aviationweather.gov — morto mas mantido caso volte

### SisGOPA Web (index.html)
1. aviationweather.gov (via proxy CORS) — morto mas mantido
2. ICAO API (GET direto, sem CORS) — fallback
- AISWEB não funciona no browser (POST + CORS bloqueado)

## Issues GitHub fechadas
- #4 (NOTAM não funciona) — fechada, commit `6f02ca7`
- #5 (luz amarela) — fechada, intermitente de rede

## Bot reiniciado
- PID 32298, com novo `fetch_notam_icao` integrado

## Regra reforçada pelo Mauro
- **NUNCA cachear dados** — tudo ao vivo, sempre. Gravado em MEMORY.md
- Bot e SisGOPA devem ser idênticos em fontes/dados
