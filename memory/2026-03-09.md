# 2026-03-09 — Sessão madrugada (00h BRT)

## Sessão de desenvolvimento — bot raio + fixes consulta

### Raio via bot (PDF) — implementado e funcionando
- Mauro envia PDF pro @SISGOP_BOT sem legenda → bot pergunta com botões 🔵 VC-1 / 🟢 VC-2
- file_id cacheado em `_raio_pending` (dict em memória) pra não estourar 64 bytes do callback_data
- Parser usa data `DD/MM/YYYY` como âncora + regex de posto pra extrair pilotos
- Bot salva `data/raio_vc1.json` ou `data/raio_vc2.json` via git push
- Commit `d56714b` — raio VC1 funcionando; `708b01e` — raio VC2 funcionando

### ESCALA V2 — comportamento do raio
- **Nomes ficam** mesmo sem consulta ativa (raio permanece até novo envio)
- **Respostas limpam** ao arquivar (respostas_pub.json → null)
- Linha revertida: `if(!c&&!raio)` — mostra "Nenhuma consulta ativa" só se não tiver raio nem consulta

### Fix crítico: git pull --rebase antes do push (sync)
- Bot estava falhando no push porque repo local ficava atrás do remote
- Adicionado `git pull --rebase origin main` antes de cada `git push` em `_sync_response_to_sheet`
- Commit `5512c9f`

### Fix: mensagem visual após confirmar
- Bot fazia `return` antes de chegar no bloco de edição de mensagem → usuário não via feedback
- Removido `return` prematuro após sync; agora cai no bloco de atualização normalmente
- Commit `428ac43`

### Fix: texto/missões da nova consulta
- Quando bot detectava nova consulta (msg_id diferente), resetava só recipients mas não atualizava texto/missões
- Agora extrai texto da mensagem Telegram e missões do teclado inline
- `consulta["recipients"] = []` (limpa tudo) em vez de iterar

### Saudação dinâmica
- Consulta agora usa "bom dia / boa tarde / boa noite" conforme horário de Brasília
- Commit `e34bd94`

### Estado atual do bot
- Rodando: `/home/node/.openclaw/workspace/sisescala/consulta_bot.py`
- Log: `/tmp/bot.log`
- Watchdog cron: `34bd0596-a7df-4378-af7d-2f7b09e69c35`
- Último commit no repo: `e34bd94`

### Pendências para próxima sessão (desktop)
- Commit `confirmed=True fix` ainda local (já no código rodando, mas verificar se foi commitado)
- Cap Sampaio ainda sem chat_id (precisa dar /start no bot)
- Apps Script para Google Sheets como data layer (precisa desktop para deploy)
- Deadline com auto-close (3h/6h/9h/12h/24h) — não implementado
- Consulta encerrada → informar usuário + telefone — não implementado

---

# 2026-03-09 — Sessão manhã (09h EDT / 10h BRT)

## Fixes aplicados e deployados

### Fix 1: filtro temMissaoDiaria (commit dfd0dad)
- `temMissaoDiaria` retornava false para todas as OMs (campo `missao` não existe mais)
- Mudado para `const temMissaoDiaria=om=>true`
- OMs nacionais agora aparecem na tela de Diárias

### Fix 2: datas "Invalid Date" (commit 2ae3ef1)
- `om.data` está em formato `DD/MM/YY` (ex: "06/03/26")
- JS interpretava como Invalid Date ao fazer `new Date(om.data+'T12:00:00')`
- Adicionadas funções helper: `parseOMDate(s, end)` e `omDataISO(s)` após linha 773
- Todos os `new Date(om.data+...)` substituídos por `parseOMDate(om.data[,true])`
- Comparadores de data atualizados

### Estado atual das OMs
- 4 diárias pendentes: 103, 96, 92, 94 — todas com prazo "VENCE HOJE" (09/03)
- OM 95 excluída do filtro nacional (perna internacional SBBR→SCVM Chile) — correto
- OM 86 é órfã: está em `concluidas` localStorage mas não existe em `om_data.json`
- OM 86 dados originais desconhecidos — usuário não forneceu

### Pop-up alertas
- Pop-up dispara após dados carregarem (fetchAllAlerts após renderAll)
- Cooldown de 1h: alertas dispensados não reaparecem por 1 hora
- Todas as 4 OMs estão no alerta "DIÁRIA VENCE HOJE" via getDiariasAlerts()
- Alerta PR viagens também presentes (alerts.json)

### Mensagem Padrão (bot)
- Usuário reportou que "não funcionou" — detalhe não confirmado
- Função botMsgPadrao usa CORS proxies (codetabs/allorigins/corsproxy.io) — não usa localhost
- Teste em andamento no momento da compactação

## Problemas pendentes reportados pelo usuário
- Desktop não carregando dados (provável cache de browser)
- Pop-up inicial não aparece no desktop
- Mensagem Padrão "não funcionou" (sem detalhes de erro)

## Regras confirmadas em vigência
- Hierarquia de missões: OM > Confirmação Bot > Diário (aba) > SAVISO
- OM duplicada no Drive → sempre usar a mais recente
- Planilha Dashboard aba "Diário" alimenta planejamento/rascunho
- OM Drive/grupo Ordens é fonte de prioridade máxima

---

## Sessão tarde (11h EDT)

### Diárias / PDF gerado → migrado para Google Sheets
- `DIARIAS_API` definida em `index.html` como constante `https://script.google.com/macros/s/AKfycbyUm_PXR8M9yU1jLhUQC60SYNF_WEUmsxsb3hMigElzBOwI2LYEGKVkXyC_A1g1bOf4/exec`
- `fetchDiariasConcluidas` agora chama DIARIAS_API (não mais GitHub)
- `fetchPdfGerado` agora chama DIARIAS_API (não mais GitHub)
- `setDiariaDownloaded` faz POST action=pdf_gerado para DIARIAS_API
- `setDiariaConcluida` confirm faz POST action=concluir para DIARIAS_API
- Antigas funções GitHub (`_saveDiariasConcluidas`, `_GH_CONC_PATH`, SHA vars) removidas
- Commits: `5bb6feb` (migração diárias + PDF para GAS), `a72dc7e` (ESCALA V2 lê de GAS)

### ESCALA V2 → migrado para CONSULTA_GAS
- `s3RenderEscalaV2` agora chama `CONSULTA_GAS` ao invés de `respostas_pub.json` do GitHub
- Nova função local `fetchGas(vc)` + helper `gasToConsulta(gas)` converte rows da planilha em estrutura `{id,text,missions,locked,recipients:[{name,responses,confirmed}]}`
- GAS espera `?action=get&vc=VC-1` e retorna `{ok:true,data:[[header...],[row...]]}`
- Commit: `a72dc7e`

### Bot (`consulta_bot.py`) → `_sync_response_to_sheet` atualizada
- Agora tenta primeiro HTTP GET para CONSULTA_GAS com params `{action:save,vc,nome,respostas,confirmado}`
- URL do GAS via env var `CONSULTA_GAS_URL` (com fallback hardcoded)
- Se GAS falhar, usa fallback antigo: git push do `data/respostas_pub.json`
- Log mostrará "GAS sync OK: <nome>" se ok, ou "GAS sync falhou" + fallback
- Antes: `GitHub sync: Mauro Machado (rc=1)` = push rejeitado → ESCALA V2 não recebia resposta

### Bug aberto: "tudo em inglês"
- Mauro reportou que resposta chegou em inglês (não há mais detalhes ainda)
- Pode ser mensagem do bot, do Apps Script, ou texto exibido no ESCALA V2
- Aguardando print/descrição para localizar origem

### Problemas pendentes
- Aeronave errada na missão de hoje (parser Diário pegando bloco errado) — OM não aparece
- Bot processo pid 645 (iniciado ~11:07 EDT)
- OMs 44, 47, 49, 98, 99, 100, 102 ainda não adicionadas ao om_data.json
- Senha S3 ainda não trocada para 12345678

---

## Sessão tarde (13h EDT)

### ESCALA V2 — fixes finais (respondendo)
- `gasToConsulta` pulava linha 0 achando que era header, mas todas as linhas são dados
- Fix: detecta header dinâmico (`rows[0][0].startsWith('Timestamp')`) — commit `c6f80f2`
- Match por nome: GAS armazena "Mauro Machado" (Telegram), raio tem "MACHADO"
- Fix: match parcial — `"MAURO MACHADO".includes("MACHADO")` → true — commit `0498338`
- Mauro confirmou "DEU CERTO" às 13:14 EDT

### Prévia ao vivo removida
- Bloco `📄 Prévia do texto:` + `<pre id="consulta-live-preview">` removido da aba consulta
- Commit `8d4df21`

### Cadastro automático de tripulantes via /start
- GAS RESPOSTA CONSULTA atualizado com novas ações: `register`, `get_tripulantes`
- Nova aba `Tripulantes` criada automaticamente na planilha com: chat_id, tg_name, posto, nome_guerra, vc, ativo, cadastrado_em
- VC buscado automaticamente via `_lookupVCfromMP(nomeGuerra)` cruzando planilha MP (ID `1EV81x0Np-...`)
- Bot `consulta_bot.py` atualizado:
  - `/start` verifica se já cadastrado; se não, pede posto + nome guerra
  - `msg_handler` processa resposta de texto e chama GAS `register`
  - `_gas_call(payload)` — função sync para chamar GAS via POST com redirect manual
- Bug corrigido: `_gas_call` era `async def` mas chamado dentro de `run_in_executor` → mudado para `def` síncrono
- Bot reiniciado às 13:25 EDT — rodando OK
- GAS: arquivo `gas_resposta_consulta_v2.js` publicado no GitHub Pages para referência
- Commit GAS: `cf70681` / `c133682`

### Estado atual do bot
- Rodando: `/home/node/.openclaw/workspace/sisescala/consulta_bot.py`
- Log: `/tmp/consulta_bot.log`
- API: `127.0.0.1:8085`, secret `sisgopa-gte-2026`

### Pendências abertas
- Mauro re-testou /start após fix mas resultado não confirmado ainda
- CONSULTA_RECIPIENTS no index.html ainda hardcoded — próximo passo: carregar dinâmico de GAS `get_tripulantes`
- OMs 44, 47, 49, 98, 99, 100, 102 ainda não adicionadas ao om_data.json
- Senha S3 ainda não trocada para 12345678
- Cron watchdog `34bd0596` com erros de timeout — não investigado

---

## Sessão tarde (14h EDT)

### Fixes implementados (commit hash em parênteses)
- `3d4ea03` — ESCALA V2: removida referência a `pub` undefined (erro na tela)
- `bdfbc95` — Consulta: prévia ao vivo restaurada (sem botão popup)
- `0bc0b8b` — ESCALA V2: coluna `#` removida (revertida a pedido do Mauro)
- `83c8a53` — ESCALA V2: resposta numérica exibe `1` em vez de `1°`
- `e4d5eb3` — Arquivar: usa `action=archive_vc` (GAS) em vez de `action=clear`
- `f1859d7` — Destinatários da consulta carregados dinamicamente da aba Tripulantes (não mais hardcoded)
- `8d17a76` — Recarrega destinatários do GAS antes de cada envio (garante lista atualizada)
- `74bf3fd` — Archive usa `archive_vc1`/`archive_vc2` (parâmetro único) pra evitar HTTP 400 no GAS GET

### Bug crítico GAS: `clearContents()` ao registrar
- Ao Osvaldo se cadastrar, aba Tripulantes foi apagada (inclusive cadastro do Mauro)
- Causa: `if(shT.getRange(1,1).getValue() !== 'chat_id') { shT.clearContents(); ... }`
- Corrigido: agora só cria header se `getLastRow() < 1` (aba vazia)
- Commit `7a65924` no gas_resposta_consulta_v2.js

### GAS v3 — estado atual
- Código: `/tmp/sisescala-tv/gas_resposta_consulta_v2.js`
- URL GAS: `https://script.google.com/macros/s/AKfycbyDdqWqCKLoCVwgajS3kr4o6q2MHx3UYxwe2o-28JbFCS__NhV2l2OqFlUT-cyRu-Vg/exec`
- Ações: `get`, `get_tripulantes`, `register`, `set_raio`, `save_response`, `archive_vc`, `archive_vc1`, `archive_vc2`
- Mauro está reimplantando progressivamente ao longo da sessão

### Problemas HTTP do GAS
- GAS GET com múltiplos parâmetros → HTTP 400 (redirect derruba params)
- Solução: usar parâmetro único por ação (ex: `archive_vc1`, `archive_vc2`)
- POST do browser → CORS bloqueado; POST server-side (python) com redirect manual funciona

### Tripulantes cadastrados (confirmados no GAS)
- MJ MACHADO (Mauro): chat_id 673591486, vc=VC-1
- MJ OSVALDO: chat_id 1022713803, vc=VC-1

### Bot consulta_bot.py — estado
- Rodando: `nohup python3 consulta_bot.py >> /tmp/consulta_bot.log`
- `_sync_response_to_sheet` usa `save_response` por `nome_guerra`
- `_lookup_nome_guerra(chat_id)` busca via GAS `get_tripulantes`
- Bot chama `archive_vc` via Python (POST server-side) quando archive passa pela API 8085
- API 8085 inacessível do browser do Mauro (GitHub Pages remoto) → cai no `_consultaApiLocal` → chama GAS via browser GET

### Testes realizados
- Consulta enviada → Mauro e Osvaldo receberam no Telegram ✅
- Ambos responderam → GAS sync OK ✅
- Archive ainda não testado com novo GAS (aguarda reimplante)

---

## Sessão tarde 2 (15h–16h EDT)

### Funcionalidades implementadas

#### PDF handler unificado (commit via bot)
- Qualquer PDF enviado ao @SISGOP_BOT agora pergunta: Raio VC-1 / Raio VC-2 / Controlão
- Removido filtro de caption `/conf` — tudo passa pelo `pdf_handler` genérico
- Handler roteia para lógica de raio ou controlão via callback `pdf_tipo|tipo|cache_key`
- `_pdf_pending` dict armazena `file_id` temporariamente

#### Confirmação de Missão — GAS separado
- Nova planilha: `SisGOPA - Confirmação` (ID: `15MyzYrdwfkX2jChz-aokq2g1mMfThayBsDMx3tGlvxA`)
- Novo GAS: `gas_confirmacao.js` — URL: `https://script.google.com/macros/s/AKfycbwAkuMtXPes8ciLZw_EYT6a4EAHz6wGwdBUj5Bqm5eM--rkO2Yj7uJy8USXTjTWNkEYhg/exec`
- Ações: `set_conf` (limpa+popula aba Confirmação + aba Meta), `conf_ciente` (marca ciente por chat_id), `get_conf` (retorna oficiais + meta)
- **Completamente independente** do GAS de consulta/raio — arquivar consulta não afeta confirmação
- Constante `CONF_GAS` e `CONF_RAW` adicionadas ao index.html
- Bot salva `data/conf_missao.json` no GitHub (raw) + chama GAS `set_conf`
- SisGOPA lê dados do controlão via GitHub raw; cientes via GAS `get_conf`
- `_conf_gas_call()` adicionada ao bot (POST + redirect manual, igual `_gas_call`)

#### ACNT/ALTR movido para submenu S3 (commit `c296f48`)
- Removido do menu principal
- Nova aba "ACNT/ALTR" dentro do S3
- `s3RenderAcnt(container)` wrapper criado

#### Sobreaviso na Confirmação (commit `058b235`)
- Seção sobreaviso na aba CONFIRMAÇÃO do S3
- Piloto selecionado do dropdown (tripulantes cadastrados no bot)
- Cada entrada tem letra de missão separada
- `_confSobreaviso` array global; add/remove com re-render
- `_enviarConfirmacao` inclui sobreaviso na chamada `send_conf`
- Bot processa sobreaviso em loop separado, conta `enviados_saviso`

#### Respostas antigas bloqueadas
- Removida lógica de "auto-criar consulta" quando ID não encontrado
- Agora resposta de consulta arquivada/inexistente: alerta ao usuário "Esta consulta já foi encerrada ou arquivada"
- Botões da mensagem antiga removidos automaticamente

### Arquitetura de dados — Confirmação
- `data/conf_missao.json` no GitHub: tripulantes, pernas, ANV, OBS, gerado_em
- GAS aba "Confirmação": posto, nome, chat_id, missao, ciente (bool), timestamp
- GAS aba "Meta": missao, anv, obs, pernas (JSON)
- Cada novo controlão substitui tudo (set_conf limpa e repopula)

### Pendências ainda abertas
- OMs 44, 47, 49, 98, 99, 100, 102 ainda faltam no om_data.json
- Senha S3 ainda não trocada para 12345678
- GAS `conf_ciente` (novo GAS, não o de consulta) — implementado e deployado ✅
- Testar fluxo completo: controlão → SisGOPA preview → enviar → CIENTE → verde

---

## Sessão tarde 3 (16h30–17h EDT)

### Fix: NaN/NaN nos "Próximos Dias" (commit 3740048)
- `getOMDates(om)` iniciava `dates` com `om.data` em formato `DD/MM/YY` (ex: "10/03/26")
- `allDates.add("10/03/26")` → `new Date("10/03/26T12:00:00")` = NaN → exibia "NaN/NaN undefined"
- Causa raiz: OMs antigas tinham datas passadas (filtradas), novas OMs (97–101) têm datas futuras → entram no allDates e quebram
- Fix: `const dates=new Set([omDataISO(om.data)])` — converte para ISO antes de adicionar ao Set
- Extra: `p.etd&&...` e `p.eta&&...` guards para pernas sem ETD/ETA; `.filter(d=>!!d&&!d.includes('undefined'))` no retorno
- Commit `3740048` — deployado via GitHub Pages

### Controlão parser + confirmação — implementados e funcionando
- `_parse_controlao(texto)` processa múltiplas OMs, filtra só futuras (ETD ≥ amanhã)
- `_build_conf_msg` renderiza por OM com tripulantes e pernas
- Bot chama `set_conf` GAS automaticamente ao processar controlão PDF
- Testes com controlão real: OMs 103 e 97 (passado/hoje) descartadas; 98, 100 incluídas ✅

### OMs adicionadas ao om_data.json (commit 41f7c21)
- OM 97: VC-2 FAB 2590, check 09/03/26 SBBR→SBBR
- OM 98: VC-1 FAB 2101, 10/03/26 SBBR local
- OM 99: VC-2 FAB 2590, 10/03/26 SBBR local
- OM 100: VC-1 FAB 2101, 10/03/26 SBBR→SCVM Chile (MJ MACHADO incluso)
- OM 101: VC-2 FAB 2590, 10/03/26 SBBR→SCVM Chile + obs sobreaviso solo SBBR
- om_data.json agora tem 10 OMs total

### GAS conf_ciente — implementado
- `gas_confirmacao.js` tem `conf_ciente` com lógica: atualiza coluna `ciente`/`timestamp` na aba Confirmação
- Bot gera callback `conf_ciente|{letra}` para cada oficial ao enviar msg de confirmação
- `_renderConfView` no SisGOPA mostra cards por oficial com badge ✅ CIENTE / ⏳ Aguardando / ⚠️ Não cadastrado

### Pendências ainda abertas no final do dia
- Teste completo do fluxo de confirmação ainda pendente
- OMs 44, 47, 49, 102 ainda faltam no om_data.json
- Senha S3 ainda não trocada para 12345678
- GAS conf_ciente ainda precisa de reimplante pelo Mauro (gas_confirmacao.js)
- Cron watchdog 34bd0596 com erros de timeout — não investigado
- SEC-1: BOT_TOKEN ainda tem fallback hardcoded no fonte
- Archive da consulta (novo GAS v3) ainda não testado

---

## Sessão final (17h EDT)

### Fix: OM 97 aparecendo como "hoje"
- Causa: havia DOIS `om_data.json` no repositório:
  - `data/om_data.json` — correto, 9 OMs (sem OM 97) — atualizado pelos commits
  - `om_data.json` (raiz) — arquivo antigo/órfão, 10 OMs (incluía OM 97)
- `loadAllData` fazia fetch `'om_data.json?v=...'` (relativo) → resolvia para raiz → pegava versão errada
- Fix 1: `git rm om_data.json` da raiz (commit `e2acf4e`)
- Fix 2: fetch corrigido para `'data/om_data.json?v=...'` (commit `6e5227c`)

### Fix: diárias quebrando com OMs novas (98–101)
- OMs novas têm pernas sem campos `origem`/`destino` — só têm `etd`/`eta` no formato `"SBBR 10/03/26 - 12:00 Z"`
- `isNacional` chamava `p.origem.startsWith(...)` → TypeError (undefined)
- Fix: funções `_icaoOrig(p)` e `_icaoDest(p)` extraem ICAO do ETD/ETA via regex quando campos ausentes (commit `3ba782f`)
- Mesma extração aplicada em `renderDiariaCard` (campo `rotas`)

### Versão atualizada
- v4.9 → **v5.0** (Mauro pediu — Confirmação de Missão + ESCALA V2 são features novas)
- Changelog adicionado ao modal de Novidades

### om_data.json — estado final do dia
- 9 OMs: 92, 94, 95 (intl/Chile), 96, 98, 99, 100, 101, 103
- OM 97 removida definitivamente (não havia missão em 09/03)
- Fetch apontando para `data/om_data.json` (corrigido)

### Commits do dia (sessão final)
- `e2acf4e` — remove om_data.json da raiz
- `eca7611` — v4.9 changelog
- `e6dbe88` — v5.0
- `3ba782f` — fix isNacional com _icaoOrig/_icaoDest
- `6e5227c` — fix fetch para data/om_data.json

### Pendências em aberto ao final do dia
- OMs 44, 47, 49, 102 ainda faltam no om_data.json
- Senha S3 ainda não trocada para 12345678
- SEC-1: BOT_TOKEN ainda tem fallback hardcoded no fonte
- Cron watchdog 34bd0596 com erros de timeout — não investigado
- Teste completo do fluxo de confirmação (controlão → ENVIAR → CIENTE → verde)
- GAS conf_ciente precisa reimplante pelo Mauro (gas_confirmacao.js)
- Archive consulta (GAS v3) ainda não testado

---

## Sessão noite (17h30–18h48 EDT)

### Metodologia de rollback implementada
- `deploy.sh` — smoke test (node --check) + copy + git commit + push; flag `--tag` cria tag estável
- `rollback.sh` — lista tags `*stable*`; extrai index.html da tag sem mudar HEAD; deploya
- Tag `v5.0-stable` criada e publicada no GitHub
- Smoke test corrigido: usar `node --check` em vez de `new Function()` (evita falsos positivos)

### Fix: card "Missões de Hoje" mostra só pilotos (commit fe9a9d9)
- Tripulação completa estava aparecendo (SO CIROMAR, 1S DANIEL, etc.)
- Filter: só mostra tripulante com trigrama de 3 letras (`/^(\S+)\s+([^(]+?)\s*\(([A-Z]{3})\s*\//`)
- Sem posto no card — só `NOME (TRI)` formato

### Fix: textos de reporte (commit 42a652a)
- "Ravi vai analisar e corrigir automaticamente" → "o Setor de Operações vai analisar e corrigir"
- "automaticamente pelo Ravi" → "pelo Setor de Operações"

### Feature: Confirmação — letra por oficial + novo texto (commit 2aae72e + 1314113)
- Cada card de oficial tem seu próprio input "Consulta:" (letra A/B/C individual)
- Campo global `conf-letra` removido; substituído por `_confLetras{}` dict por nome
- Texto da mensagem: `"Prezado tripulante, informo que o senhor está escalado para a consulta X"`
- `_buildMsgOficial` unificada (sem duplicata); filtra só OMs do destinatário
- `_enviarConfirmacao` passa `letras_por_oficial:_confLetras` para a API
- Bot `_build_conf_msg` também usa novo texto e aplica letra por oficial
- Fix: botão ENVIAR CONFIRMAÇÃO não funcionava — validação de `conf-letra` removida (campo inexistente)
- Crash causado por syntax error no primeiro deploy → revert imediato via `git revert HEAD`
- Segundo deploy validado com `node --check` → OK

### Fix: confirmação filtra OMs por destinatário (commit 4ee9a65)
- `_buildMsgOficial` (frontend) e `_build_conf_msg` (bot) agora filtram só as OMs em que o nome do oficial aparece na tripulação
- Fallback: se não encontrar em nenhuma OM específica, mostra todas

### Estado atual do bot (18h48 EDT)
- PID 8302, rodando OK
- Commits do dia (noite): fe9a9d9, 42a652a, 4ee9a65, 2aae72e, 1314113

### Pendências ainda abertas
- Teste completo do fluxo de confirmação (controlão → ENVIAR → oficial recebe → CIENTE → verde no SisGOPA)
- GAS conf_ciente precisa reimplante pelo Mauro
- OMs 44, 47, 49 ainda faltam no om_data.json
- Senha S3 ainda não trocada para 12345678
- SEC-1: BOT_TOKEN ainda tem fallback hardcoded
- Cron watchdog 34bd0596 com erros de timeout — não investigado

## Sessão continuação — 19:26 EDT

### Bugs corrigidos
- **s3ActiveTab typo**: default era `'escala_v2'` (underscore) mas tab usa `'escalav2'` (sem underscore) — nunca batia, ESCALA V2 renderizava em branco; commit `55f0d01`
- **ESCALA legacy removida**: tab ESCALA antiga removida do S3 menu; `-172 linhas`; commit `019a341`

### Status confirmado
- GAS CONSULTA retornando dados corretamente: VC-1 (10 pilotos), VC-2 (16 pilotos)
- `set_raio` funcionando — Mauro enviou raio via bot, dados chegaram no GAS
- Dashboard carregando normalmente: 26 tripulantes, 20 disponíveis, METAR OK

### Ainda pendente
- Mauro precisa reimplantar `gas_confirmacao.js` (CONF_GAS) com `send_conf` action
- `conf_ciente` ainda não implementado no CONSULTA_GAS
- OMs 44, 47, 49 faltam no om_data.json
- Senha S3 ainda não trocada para `12345678`
- SEC-1: BOT_TOKEN ainda tem fallback hardcoded no bot.py
